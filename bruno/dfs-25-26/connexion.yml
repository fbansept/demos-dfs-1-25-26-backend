info:
  name: connexion
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/connexion
  body:
    type: json
    data: |-
      {
        "email": "a@a.com",
        "password" : "root"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
